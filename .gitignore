# macOS / design-tool cruft
.DS_Store
.thumbnail

# build-tool screenshots and uploads (not part of the published site)
uploads/
screenshots/

# superseded versions, intermediates, and internal docs — kept locally, not shared
# (originals + edit-pass v1, the tour lab, the build handoff, the 00-system meta page)
archived/
